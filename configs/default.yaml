model:
  d_model: 128
  nhead: 8
  num_layers: 4
  dim_feedforward: 256
  dropout: 0.1

training:
  batch_size: 256
  learning_rate: 3.0e-4
  weight_decay: 1.0e-4
  num_epochs: 200
  warmup_epochs: 5
  lambda_adv: 1.0
  lambda_adv_rampup: 10

data:
  normalize_by_ht: true
  num_jets: 6
